// "use client";

import Hero from "@/components/Hero";
import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <main>
      <Hero />
    </main>
  );
};

export default Home;
